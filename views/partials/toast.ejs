<div
  x-data="{ toasts: [] }"
  x-on:toast.window="const t = { id: Date.now(), message: $event.detail.message, type: $event.detail.type || 'info' }; toasts.push(t); setTimeout(() => { toasts = toasts.filter(x => x.id !== t.id) }, 4000)"
  class="fixed top-20 right-4 z-50 space-y-2"
  aria-live="polite"
>
  <template x-for="t in toasts" :key="t.id">
    <div
      x-show="true"
      x-transition:enter="transition ease-out duration-200"
      x-transition:enter-start="opacity-0 translate-x-8"
      x-transition:enter-end="opacity-100 translate-x-0"
      :class="{
        'bg-emerald-900/90 border-emerald-600': t.type === 'success',
        'bg-red-900/90 border-red-600': t.type === 'error',
        'bg-slate-700/90 border-slate-600': t.type === 'info'
      }"
      class="px-4 py-3 rounded-lg border shadow-lg text-slate-100"
      x-text="t.message"
    ></div>
  </template>
</div>
